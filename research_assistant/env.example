## Research Assistant configuration
## Copy to `.env` (same directory) and fill values.

# Required (Claude / Anthropic)
ANTHROPIC_API_KEY=sk-ant-...
CLAUDE_MODEL=claude-sonnet-4-20250514

# Optional (web search). Without this, the system will fall back to official RSS/blog feeds where possible.
# SEARCH_PROVIDER can be: tavily | serper
SEARCH_PROVIDER=tavily
SEARCH_API_KEY=

# Runtime tuning
MAX_ITERATIONS=10
TIMEOUT=30

# Optional (email delivery)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=
EMAIL_TO=

